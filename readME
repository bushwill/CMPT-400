Program Structure:

DFABuilder_Main.py
    -> DFABuilder_Library.py
        -> FAdo.fa
    -> DFABuilder_OpenAI_Interface.py
        -> openai
            -> openai api-key
                -> openai project account

Dependencies:
    -> FAdo.fa
        -> GraphViz
    -> openai 

Tasks:
    -> Find a better system instruction for the GPT instance
    -> Function to test DFA validity
    -> Switch api-key to university account with payment set up
    -> K-Fold cross validation
    -> Generate positive examples from a regex and compare differences in represented languages between the input regex and output dfa?